apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      nodePort: 30002
  type: NodePort
  selector:
    app: frontend
